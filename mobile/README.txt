Per eseguire questa parte del progetto è necessario, dopo aver installato cordova, Nodejs, npm e l'Android SDK digitare i seguenti comandi:
- npm install
- cordova run (per eseguire l'applicazione)
- cordova build (per generare solamente il file apk)